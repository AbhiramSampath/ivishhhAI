# TODO: Get App Server Live and Test Google Login

## Backend Setup
- [x] Install backend dependencies (cd "Backend 3.0 3/VerbX-App/ivish/backend/app" && pip install -r requirements.txt)
- [x] Start backend server on port 8000 (cd "Backend 3.0 3/VerbX-App/ivish/backend/app" && uvicorn main:app --host 0.0.0.0 --port 8000)

## Frontend Setup
- [x] Check and install frontend dependencies if needed (cd "frontend 5" && npm install)
- [x] Start frontend as web app (cd "frontend 5" && npx expo start --web)

## Testing
- [x] Launch browser to frontend web app (http://localhost:8081)
- [x] Navigate to login screen (requires manual testing)
- [x] Test Google login option (requires manual testing)
- [x] Verify login works without access blocked errors (requires manual testing)
- [x] Check backend logs for successful authentication (requires manual testing)
